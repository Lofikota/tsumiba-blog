---
import { getCollection } from 'astro:content';
const siteUrl = 'https://ren-money.com';
const allPosts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
type CatStyle = { cls: string; bg: string };
const catStyle: Record<string, CatStyle> = {
  'NISA・投資': { cls: 'cat-invest',  bg: 'linear-gradient(135deg,#DBEAFE,#93C5FD)' },
  '副業・節税': { cls: 'cat-income',  bg: 'linear-gradient(135deg,#D4EDDA,#86EFAC)' },
  'お得情報':   { cls: 'cat-savings', bg: 'linear-gradient(135deg,#FEF3C7,#FDE68A)' },
  'FX・外貨':   { cls: 'cat-debt',    bg: 'linear-gradient(135deg,#FFE4E6,#FCA5A5)' },
};
---
<!doctype html>
<html lang="ja">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>田中蓮のマネーブログ | 副業・NISA・節税でFIREを目指す32歳のリアル体験談。</title>
  <meta name="description" content="借金200万→副業月20万→資産500万。お金の知識で人生を立て直した体験談ベースのメディア。NISA・副業・節税を初心者向けにわかりやすく解説。"/>
  <meta name="robots" content="index,follow"/>
  <link rel="canonical" href={`${siteUrl}/`}/>
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700;900&family=Noto+Serif+JP:wght@700;900&display=swap" rel="stylesheet"/>

  <style is:global>
  /* ═══════════════════════════════════════
     DESIGN TOKENS — 田中蓮のマネーブログ
  ═══════════════════════════════════════ */
  :root {
    /* Core palette */
    --ink:        #0A0A0A;
    --paper:      #F6F3EC;
    --navy:       #0B1942;
    --navy-mid:   #162254;
    --amber:      #B8780E;
    --amber-warm: #E8940F;
    --amber-pale: #FEF3D0;
    --rust:       #C0392B;
    --sage:       #1E6B45;
    --sage-pale:  #D4EDDA;
    --white:      #FFFFFF;
    --cream:      #FBF8F2;
    --mist:       #EEF1F7;
    --border:     #D9D4CA;
    --gray:       #6B6560;
    --gray-light: #F0EDE8;

    /* Typography */
    --serif: "Noto Serif JP", "Hiragino Mincho ProN", serif;
    --sans:  "Noto Sans JP", "Hiragino Kaku Gothic ProN", sans-serif;

    /* Radii */
    --r-xs: 3px;
    --r-sm: 6px;
    --r-md: 10px;
    --r-lg: 16px;

    /* Shadows */
    --sh-sm: 0 1px 3px rgba(10,10,10,.08), 0 4px 10px rgba(10,10,10,.06);
    --sh-md: 0 4px 16px rgba(10,10,10,.10), 0 12px 32px rgba(10,10,10,.06);
    --sh-amber: 0 4px 20px rgba(184,120,14,.30);
  }

  /* ── Reset ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body {
    font-family: var(--sans);
    color: var(--ink);
    background: var(--white);
    font-size: 15px;
    line-height: 1.75;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }
  img { max-width: 100%; display: block; }
  a   { color: inherit; text-decoration: none; }
  button { font-family: inherit; cursor: pointer; border: none; background: none; }

  /* ── Layout ── */
  .container { max-width: 1080px; margin: 0 auto; padding: 0 28px; }
  .container--narrow { max-width: 780px; margin: 0 auto; padding: 0 28px; }
  .section { padding: 72px 0; }
  .section--tint { background: var(--cream); }
  .section--dark  { background: var(--navy); color: #fff; }
  .section--paper { background: var(--paper); }

  /* ── Editorial heading ── */
  .editorial-label {
    display: inline-flex; align-items: center; gap: 10px;
    font-size: 11px; font-weight: 700; letter-spacing: .18em;
    text-transform: uppercase; color: var(--amber);
  }
  .editorial-label::before,
  .editorial-label::after {
    content: ''; display: block;
    width: 24px; height: 1px; background: var(--amber);
  }
  .section-title {
    font-family: var(--serif);
    font-size: clamp(22px, 3.5vw, 30px);
    font-weight: 900; line-height: 1.35;
    margin-top: 10px;
  }
  .section-sub {
    margin-top: 8px;
    color: var(--gray);
    font-size: 13.5px;
    line-height: 1.8;
  }
  .section-head { text-align: center; margin-bottom: 48px; }

  /* ═══════════════════════════════════════
     ANNOUNCEMENT BAR
  ═══════════════════════════════════════ */
  .announce-bar {
    background: var(--amber-pale);
    border-bottom: 1px solid #E8D4A0;
    padding: 9px 24px;
    font-size: 11.5px;
    color: #6B4A0A;
    text-align: center;
    line-height: 1.5;
    font-weight: 500;
  }
  .announce-bar strong { color: var(--amber); }

  /* ═══════════════════════════════════════
     HEADER — Editorial newspaper style
  ═══════════════════════════════════════ */
  .site-header {
    background: var(--white);
    border-bottom: 3px solid var(--ink);
    position: sticky; top: 0; z-index: 60;
  }
  .header-inner {
    display: flex; align-items: center;
    justify-content: space-between;
    height: 58px;
  }
  .brand {
    display: inline-flex; align-items: center; gap: 9px;
    border-right: 1px solid var(--border);
    padding-right: 20px;
    margin-right: 6px;
  }
  .brand-text {
    font-family: var(--serif);
    font-weight: 700; font-size: 15px;
    color: var(--ink); letter-spacing: .03em;
    white-space: nowrap;
  }
  .nav { display: flex; gap: 24px; }
  .nav-link {
    font-size: 13px; font-weight: 500;
    color: var(--ink); opacity: .75;
    transition: opacity .15s;
  }
  .nav-link:hover { opacity: 1; }
  .header-right {
    display: flex; flex-direction: column;
    align-items: flex-end; line-height: 1.25;
  }
  .header-pr { font-size: 10px; color: var(--gray); letter-spacing: .05em; }

  /* ═══════════════════════════════════════
     HERO — Split-panel editorial
     Left: deep navy  |  Right: amber warm
  ═══════════════════════════════════════ */
  .hero {
    display: grid;
    grid-template-columns: 55% 45%;
    min-height: 88vh;
    overflow: hidden;
    position: relative;
  }

  /* Left panel: dark editorial */
  .hero-left {
    background: var(--navy);
    padding: 72px 56px 72px 72px;
    display: flex; flex-direction: column; justify-content: center;
    position: relative; overflow: hidden;
  }
  /* Halftone dot texture */
  .hero-left::before {
    content: '';
    position: absolute; inset: 0;
    background-image: radial-gradient(circle, rgba(255,255,255,.04) 1px, transparent 1px);
    background-size: 22px 22px;
    pointer-events: none;
  }
  /* Decorative vertical line */
  .hero-left::after {
    content: '';
    position: absolute; right: 0; top: 15%; bottom: 15%;
    width: 3px;
    background: linear-gradient(to bottom, transparent, var(--amber-warm), transparent);
  }

  .hero-issue {
    font-size: 10.5px; font-weight: 700; letter-spacing: .22em;
    color: var(--amber-warm); text-transform: uppercase;
    margin-bottom: 28px;
    opacity: 0;
    animation: fadeUp .6s ease forwards;
    animation-delay: .1s;
  }

  .hero-h1 {
    font-family: var(--serif);
    font-size: clamp(32px, 4.5vw, 56px);
    font-weight: 900;
    line-height: 1.25;
    letter-spacing: .03em;
    color: #fff;
    margin-bottom: 24px;
    opacity: 0;
    animation: fadeUp .7s ease forwards;
    animation-delay: .25s;
  }
  .hero-h1 em {
    font-style: normal;
    color: var(--amber-warm);
    position: relative;
    display: inline-block;
  }
  .hero-h1 em::after {
    content: '';
    position: absolute; left: 0; bottom: -2px;
    width: 100%; height: 2px;
    background: var(--amber-warm); opacity: .6;
  }

  .hero-lead {
    font-size: 14px; color: rgba(255,255,255,.72);
    line-height: 1.9; margin-bottom: 32px;
    opacity: 0;
    animation: fadeUp .7s ease forwards;
    animation-delay: .4s;
  }
  .hero-disclaimer {
    font-size: 10px; color: rgba(255,255,255,.4);
    display: block; margin-top: 8px;
    letter-spacing: .02em;
  }

  .hero-cta-row {
    display: flex; gap: 12px; flex-wrap: wrap;
    margin-bottom: 40px;
    opacity: 0;
    animation: fadeUp .7s ease forwards;
    animation-delay: .55s;
  }

  /* Achievement mini-row */
  .hero-stats {
    display: flex; gap: 0;
    border-top: 1px solid rgba(255,255,255,.12);
    padding-top: 24px;
    opacity: 0;
    animation: fadeUp .7s ease forwards;
    animation-delay: .7s;
  }
  .hero-stat {
    flex: 1;
    padding: 0 20px 0 0;
    border-right: 1px solid rgba(255,255,255,.12);
    margin-right: 20px;
  }
  .hero-stat:last-child { border-right: none; margin-right: 0; }
  .hero-stat-num {
    font-family: var(--serif);
    font-size: 26px; font-weight: 900;
    color: var(--amber-warm);
    line-height: 1;
    display: block;
  }
  .hero-stat-label {
    font-size: 10.5px; color: rgba(255,255,255,.55);
    margin-top: 4px; line-height: 1.4;
    display: block;
  }

  /* Right panel: amber manga character */
  .hero-right {
    background: var(--amber-pale);
    display: flex; align-items: flex-end; justify-content: center;
    position: relative; overflow: hidden;
    padding-bottom: 0;
  }
  /* Manga screen tone */
  .hero-right::before {
    content: '';
    position: absolute; inset: 0;
    background-image: radial-gradient(circle, rgba(184,120,14,.10) 1.5px, transparent 1.5px);
    background-size: 14px 14px;
    pointer-events: none;
  }
  /* Speed-lines from top corner */
  .hero-right::after {
    content: '';
    position: absolute; top: 0; left: 0; right: 0; bottom: 0;
    background: repeating-linear-gradient(
      -30deg,
      transparent 0px,
      transparent 40px,
      rgba(184,120,14,.04) 40px,
      rgba(184,120,14,.04) 41px
    );
    pointer-events: none;
  }

  .hero-char-wrap {
    position: relative; z-index: 2;
    padding: 40px 20px 0;
    opacity: 0;
    animation: charReveal .8s cubic-bezier(.34,1.56,.64,1) forwards;
    animation-delay: .5s;
    max-width: 320px; width: 100%;
  }
  .hero-char-caption {
    position: absolute;
    top: 24px; left: 50%; transform: translateX(-50%);
    background: var(--navy);
    color: #fff;
    font-family: var(--serif);
    font-size: 12px; font-weight: 700;
    padding: 6px 18px;
    border-radius: var(--r-pill);
    white-space: nowrap;
    border: 2px solid var(--amber-warm);
    z-index: 3;
  }

  /* ═══════════════════════════════════════
     BUTTONS
  ═══════════════════════════════════════ */
  .btn {
    display: inline-flex; align-items: center;
    gap: 10px; padding: 13px 24px;
    border-radius: var(--r-sm);
    font-weight: 700; font-size: 14px;
    line-height: 1;
    transition: transform .15s ease, box-shadow .15s ease, background .15s ease;
    white-space: nowrap; cursor: pointer;
  }
  .btn:hover { transform: translateY(-2px); }

  .btn-primary {
    background: var(--amber-warm);
    color: var(--ink);
    box-shadow: 0 3px 0 var(--amber), var(--sh-amber);
  }
  .btn-primary:hover { background: #F0A500; box-shadow: 0 4px 0 var(--amber), 0 6px 24px rgba(184,120,14,.4); }

  .btn-navy {
    background: var(--navy);
    color: #fff;
    box-shadow: 0 3px 0 #040C26;
  }
  .btn-navy:hover { background: var(--navy-mid); }

  .btn-ghost {
    background: transparent;
    color: #fff;
    border: 1.5px solid rgba(255,255,255,.5);
  }
  .btn-ghost:hover { background: rgba(255,255,255,.08); border-color: #fff; }

  .btn-sage {
    background: var(--sage);
    color: #fff;
    box-shadow: 0 3px 0 #154D31;
  }

  .btn-block { width: 100%; justify-content: center; }
  .btn-lg { padding: 16px 32px; font-size: 15px; }
  .btn .arrow { font-size: 16px; }

  /* ═══════════════════════════════════════
     SUB-BANNER
  ═══════════════════════════════════════ */
  .subbanner-wrap {
    position: relative; z-index: 10;
    margin-top: -32px;
  }
  .subbanner {
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: var(--r-lg);
    box-shadow: var(--sh-md);
    padding: 20px 28px;
    display: flex; align-items: center; justify-content: space-between; gap: 24px;
  }
  .sb-left { display: flex; align-items: center; gap: 16px; }
  .sb-icon {
    width: 52px; height: 52px; border-radius: var(--r-md);
    background: var(--paper); border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center; flex-shrink: 0;
  }
  .sb-title { font-family: var(--serif); font-size: 15px; font-weight: 700; }
  .sb-sub   { font-size: 12px; color: var(--gray); margin-top: 2px; }

  /* ═══════════════════════════════════════
     PROBLEM SECTION — こんな悩みありませんか？
  ═══════════════════════════════════════ */
  .problem-grid {
    display: grid;
    grid-template-columns: repeat(3,1fr);
    gap: 16px; margin-top: 12px;
  }
  .problem-card {
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: var(--r-md);
    padding: 22px 20px;
    position: relative;
    transition: transform .2s, box-shadow .2s;
  }
  .problem-card:hover { transform: translateY(-3px); box-shadow: var(--sh-md); }
  .problem-card::before {
    content: '';
    position: absolute; left: 0; top: 16px; bottom: 16px;
    width: 3px; background: var(--rust);
    border-radius: 2px;
  }
  .problem-num {
    font-family: var(--serif);
    font-size: 44px; font-weight: 900;
    color: var(--paper);
    line-height: 1;
    position: absolute; right: 16px; top: 10px;
    pointer-events: none;
  }
  .problem-icon { font-size: 28px; margin-bottom: 10px; }
  .problem-title { font-size: 15px; font-weight: 700; margin-bottom: 6px; line-height: 1.4; }
  .problem-body  { font-size: 12.5px; color: var(--gray); line-height: 1.7; }

  /* ═══════════════════════════════════════
     STORY — 4コマ manga editorial (redesigned)
  ═══════════════════════════════════════ */
  .manga-grid {
    display: grid;
    grid-template-columns: repeat(4,1fr);
    gap: 0;
    border: 3px solid var(--ink);
    border-radius: var(--r-md);
    overflow: hidden;
    margin-top: 40px;
    box-shadow: 5px 5px 0 var(--navy-mid);
  }
  .manga-panel {
    background: var(--white);
    display: flex; flex-direction: column;
    border-right: 3px solid var(--ink);
    position: relative;
  }
  .manga-panel:last-child { border-right: none; }

  .manga-panel-head {
    padding: 9px 13px;
    border-bottom: 2px solid var(--ink);
    display: flex; align-items: center; gap: 8px;
    background: var(--white);
    flex-shrink: 0;
  }
  .manga-num {
    width: 26px; height: 26px;
    background: var(--ink); color: var(--white);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-weight: 900; font-size: 13px; flex-shrink: 0;
  }
  .manga-panel-title { font-size: 13px; font-weight: 700; line-height: 1.3; }

  /* Scene area: colored background + character */
  .manga-scene-bg {
    flex: 1;
    min-height: 220px;
    display: flex; flex-direction: column;
    align-items: center; justify-content: flex-end;
    padding: 16px 12px 10px;
    position: relative;
    overflow: hidden;
  }
  .manga-char-svg {
    display: block;
    width: 90px;
    max-width: 75%;
    margin: 0 auto;
    position: relative; z-index: 2;
  }
  .manga-prop {
    position: absolute;
    top: 12px; left: 12px;
    z-index: 3;
  }
  .manga-prop-tr {
    position: absolute;
    top: 10px; right: 12px;
    z-index: 3;
  }

  /* Speech section — below scene, full width, big readable text */
  .manga-speech {
    border-top: 2px solid var(--ink);
    padding: 13px 14px 14px;
    font-size: 13px;
    font-weight: 700;
    line-height: 1.65;
    color: var(--ink);
    background: var(--white);
    flex-shrink: 0;
  }
  .manga-speech--dark {
    background: var(--navy);
    color: rgba(255,255,255,.88);
    border-top-color: rgba(255,255,255,.2);
  }

  /* Panel connector arrows */
  .manga-connector {
    position: absolute;
    right: -15px; top: 48%;
    transform: translateY(-50%);
    z-index: 10;
    width: 28px; height: 28px;
    background: var(--navy);
    border: 2px solid var(--white);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: #fff; font-size: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,.3);
  }

  /* Illustrated 4コマ strip (image-based) */
  .manga-img-wrap {
    margin-top: 40px;
    border: 3px solid var(--ink);
    border-radius: var(--r-md);
    overflow: hidden;
    box-shadow: 5px 5px 0 var(--navy-mid);
    background: var(--white);
  }
  .manga-strip-img {
    display: block;
    width: 100%;
    height: auto;
  }

  /* ═══════════════════════════════════════
     THEMES — 4-card grid
  ═══════════════════════════════════════ */
  .themes-grid {
    display: grid;
    grid-template-columns: repeat(4,1fr);
    gap: 16px;
    margin-top: 12px;
  }
  .theme-card {
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: var(--r-md);
    padding: 24px 20px 22px;
    transition: transform .2s, box-shadow .2s, border-color .2s;
  }
  .theme-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--sh-md);
    border-color: var(--amber);
  }
  .theme-icon-wrap {
    width: 48px; height: 48px;
    background: var(--paper);
    border-radius: var(--r-sm);
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 14px;
    border: 1px solid var(--border);
  }
  .theme-title { font-size: 15px; font-weight: 700; margin-bottom: 6px; }
  .theme-body  { font-size: 12px; color: var(--gray); line-height: 1.7; }

  /* ═══════════════════════════════════════
     ARTICLES — 3-col editorial grid
  ═══════════════════════════════════════ */
  .articles-grid {
    display: grid;
    grid-template-columns: repeat(3,1fr);
    gap: 20px;
    margin-top: 12px;
  }
  .article-card {
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: var(--r-md);
    overflow: hidden;
    transition: transform .2s, box-shadow .2s;
  }
  .article-card:hover { transform: translateY(-3px); box-shadow: var(--sh-md); }

  .article-thumb {
    aspect-ratio: 16/10;
    overflow: hidden;
    position: relative;
  }
  .article-thumb svg { position: absolute; inset: 0; width: 100%; height: 100%; }

  .article-body { padding: 16px 18px 18px; }
  .article-cat {
    display: inline-block;
    font-size: 10.5px; font-weight: 700;
    padding: 3px 10px;
    border-radius: var(--r-pill);
    margin-bottom: 10px;
    letter-spacing: .03em;
  }
  .cat-savings  { background: #FEF3C7; color: #92400E; }
  .cat-invest   { background: var(--mist); color: var(--navy); }
  .cat-income   { background: var(--sage-pale); color: #155234; }
  .cat-debt     { background: #FFE4E6; color: #9B1C1C; }
  .cat-tax      { background: #EDE9FE; color: #4C1D95; }

  .article-title {
    font-size: 13.5px; font-weight: 700;
    line-height: 1.55; color: var(--ink);
    margin-bottom: 12px;
  }
  .article-meta {
    display: flex; align-items: center; gap: 5px;
    font-size: 11px; color: var(--gray);
  }

  /* ═══════════════════════════════════════
     REASONS — 4-card editorial
  ═══════════════════════════════════════ */
  .reasons-grid {
    display: grid;
    grid-template-columns: repeat(4,1fr);
    gap: 16px;
    margin-top: 12px;
  }
  .reason-card {
    background: var(--paper);
    border-radius: var(--r-md);
    padding: 24px 18px;
    text-align: center;
    border: 1.5px solid var(--border);
  }
  .reason-badge {
    display: inline-block;
    background: var(--navy);
    color: #fff;
    font-size: 10px; font-weight: 700;
    padding: 4px 12px; border-radius: var(--r-pill);
    letter-spacing: .04em; margin-bottom: 14px;
  }
  .reason-icon-wrap {
    width: 64px; height: 64px;
    background: var(--white); border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 12px; overflow: hidden;
    border: 1px solid var(--border);
  }
  .reason-text { font-size: 12px; color: var(--ink); line-height: 1.65; }

  /* ═══════════════════════════════════════
     ACHIEVEMENT BAR
  ═══════════════════════════════════════ */
  .achievement-bar {
    background: var(--ink);
    padding: 40px 0;
  }
  .achievement-inner {
    display: flex; align-items: center;
    justify-content: center; gap: 0;
  }
  .achievement-item {
    flex: 1; text-align: center;
    padding: 0 32px;
    border-right: 1px solid rgba(255,255,255,.1);
    max-width: 220px;
  }
  .achievement-item:last-child { border-right: none; }
  .achievement-num {
    font-family: var(--serif);
    font-size: clamp(36px, 5vw, 52px);
    font-weight: 900;
    color: var(--amber-warm);
    line-height: 1;
    display: block;
  }
  .achievement-label {
    font-size: 12px; color: rgba(255,255,255,.55);
    margin-top: 6px; display: block;
  }

  /* ═══════════════════════════════════════
     COMPARE TABLE
  ═══════════════════════════════════════ */
  .compare-wrap { overflow-x: auto; margin-top: 12px; }
  .compare-table {
    width: 100%; border-collapse: collapse;
    border: 1.5px solid var(--border);
    border-radius: var(--r-md);
    overflow: hidden;
    font-size: 13px;
    min-width: 600px;
  }
  .compare-table thead tr { background: var(--navy); color: #fff; }
  .compare-table th {
    padding: 13px 16px; font-weight: 700; font-size: 12px;
    letter-spacing: .03em; text-align: center;
  }
  .compare-table th:first-child { text-align: left; }
  .compare-table td {
    padding: 13px 16px; border-top: 1px solid var(--border);
    text-align: center;
  }
  .compare-table td:first-child {
    text-align: left; font-weight: 700;
    background: var(--paper);
  }
  .compare-table tr:hover td { background: var(--cream); }
  .compare-table tr:hover td:first-child { background: #EEEBE3; }
  .highlight-cell {
    background: var(--amber-pale) !important;
    font-weight: 700; color: #7A4A00;
  }
  .btn-compare {
    display: inline-flex; align-items: center; gap: 4px;
    background: var(--amber-warm); color: var(--ink);
    padding: 7px 14px; border-radius: var(--r-sm);
    font-size: 11.5px; font-weight: 700;
    transition: background .15s;
    box-shadow: 0 2px 0 var(--amber);
  }
  .btn-compare:hover { background: #F0A500; }

  /* ═══════════════════════════════════════
     FAQ
  ═══════════════════════════════════════ */
  .faq-list { display: flex; flex-direction: column; gap: 8px; margin-top: 12px; }
  .faq-item {
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: var(--r-md); overflow: hidden;
    transition: border-color .15s;
  }
  .faq-item[open] { border-color: var(--amber); }
  .faq-item summary {
    display: flex; align-items: center; gap: 12px;
    padding: 16px 20px;
    font-size: 14px; font-weight: 700;
    cursor: pointer; list-style: none;
    transition: background .15s;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary:hover { background: var(--cream); }
  .faq-q-icon {
    width: 26px; height: 26px; flex-shrink: 0;
    background: var(--navy); color: #fff;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 12px; font-weight: 900;
  }
  .faq-q-text { flex: 1; }
  .faq-toggle {
    width: 22px; height: 22px;
    background: var(--paper); border: 1px solid var(--border);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 16px; font-weight: 700; flex-shrink: 0;
    color: var(--amber); transition: transform .2s;
  }
  .faq-item[open] .faq-toggle { transform: rotate(45deg); }
  .faq-a {
    padding: 0 20px 16px 60px;
    font-size: 13.5px; color: var(--gray); line-height: 1.8;
  }

  /* ═══════════════════════════════════════
     FINAL CTA
  ═══════════════════════════════════════ */
  .finalcta {
    background: var(--navy);
    padding: 80px 0;
    position: relative; overflow: hidden;
  }
  .finalcta::before {
    content: '';
    position: absolute; inset: 0;
    background-image: radial-gradient(circle, rgba(255,255,255,.03) 1px, transparent 1px);
    background-size: 28px 28px;
  }
  .finalcta-inner {
    position: relative; z-index: 2;
    text-align: center; max-width: 680px; margin: 0 auto;
  }
  .finalcta-eyebrow {
    font-size: 11px; font-weight: 700; letter-spacing: .18em;
    color: var(--amber-warm); text-transform: uppercase;
    margin-bottom: 16px; display: block;
  }
  .finalcta-h {
    font-family: var(--serif);
    font-size: clamp(26px, 4vw, 38px);
    font-weight: 900; color: #fff;
    line-height: 1.35; margin-bottom: 14px;
  }
  .finalcta-sub { font-size: 14px; color: rgba(255,255,255,.65); margin-bottom: 36px; }
  .finalcta-row {
    display: flex; align-items: center; justify-content: center;
    gap: 12px; flex-wrap: wrap;
  }

  /* ═══════════════════════════════════════
     FOOTER
  ═══════════════════════════════════════ */
  .footer { background: #080E21; color: #A8B4CC; padding-top: 40px; }
  .footer-grid {
    display: grid; grid-template-columns: repeat(4,1fr);
    gap: 28px; padding-bottom: 32px;
  }
  .footer-h {
    font-family: var(--serif);
    color: #fff; font-weight: 700; font-size: 13px;
    margin-bottom: 12px; padding-bottom: 8px;
    border-bottom: 1px solid rgba(255,255,255,.08);
  }
  .footer-list { list-style: none; display: flex; flex-direction: column; gap: 7px; font-size: 12px; }
  .footer-list a:hover { color: #fff; }
  .footer-bottom { border-top: 1px solid rgba(255,255,255,.08); padding: 16px 0; }
  .footer-bottom-inner { display: flex; align-items: center; justify-content: space-between; font-size: 11px; }
  .footer-brand { display: flex; align-items: center; gap: 8px; color: #fff; font-weight: 700; font-family: var(--serif); }
  .footer-copy  { color: #5A6478; }

  /* ═══════════════════════════════════════
     STICKY CTA (mobile)
  ═══════════════════════════════════════ */
  .sticky-cta {
    display: none;
    position: fixed; bottom: 0; left: 0; right: 0;
    background: var(--white);
    border-top: 2px solid var(--amber-warm);
    padding: 10px 16px;
    z-index: 70;
    box-shadow: 0 -4px 20px rgba(0,0,0,.12);
    backdrop-filter: blur(8px);
  }
  .sticky-cta .btn { width: 100%; justify-content: center; padding: 15px; font-size: 15px; }

  /* ═══════════════════════════════════════
     CHIP / BADGE
  ═══════════════════════════════════════ */
  .chip {
    display: inline-flex; align-items: center;
    padding: 4px 12px; border-radius: var(--r-pill);
    font-size: 11px; font-weight: 700;
  }

  /* ═══════════════════════════════════════
     ANIMATIONS
  ═══════════════════════════════════════ */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  @keyframes charReveal {
    from { opacity: 0; transform: translateY(40px) scale(.96); }
    to   { opacity: 1; transform: translateY(0) scale(1); }
  }
  @keyframes shimmer {
    0%,100% { opacity: .6; }
    50% { opacity: 1; }
  }

  /* Scroll-reveal base (JS enhances with .is-visible) */
  .reveal {
    opacity: 0; transform: translateY(24px);
    transition: opacity .6s ease, transform .6s ease;
  }
  .reveal.is-visible { opacity: 1; transform: translateY(0); }

  /* ═══════════════════════════════════════
     RESPONSIVE
  ═══════════════════════════════════════ */
  @media (max-width: 960px) {
    .hero { grid-template-columns: 1fr; min-height: auto; }
    .hero-left { padding: 56px 28px 40px; }
    .hero-right { min-height: 320px; }
    .themes-grid, .reasons-grid, .footer-grid { grid-template-columns: 1fr 1fr; }
    .articles-grid { grid-template-columns: 1fr 1fr; }
    .problem-grid  { grid-template-columns: 1fr; }
    .manga-grid    { grid-template-columns: 1fr 1fr; gap: 2px; }
    .manga-connector { display: none; }
    .achievement-inner { flex-wrap: wrap; gap: 32px; justify-content: center; }
    .nav { display: none; }
    .sticky-cta { display: block; }
    body { padding-bottom: 72px; }
  }
  @media (max-width: 600px) {
    .hero-h1 { font-size: 28px; }
    .hero-stats { flex-direction: column; gap: 16px; }
    .hero-stat { border-right: none; padding-right: 0; }
    .themes-grid, .reasons-grid { grid-template-columns: 1fr; }
    .articles-grid { grid-template-columns: 1fr; }
    .footer-grid { grid-template-columns: 1fr 1fr; gap: 20px; }
    .manga-grid { grid-template-columns: 1fr 1fr; }
    .finalcta-row { flex-direction: column; }
    .finalcta-row .btn { width: 100%; max-width: 360px; }
    .subbanner { flex-direction: column; gap: 12px; }
  }
  </style>
</head>
<body>

  <!-- PR開示バー -->
  <div class="announce-bar">
    <strong>PR</strong> 当サイトはアフィリエイト広告を含みます。紹介するサービスは実際に調査したものです。
    投資・副業にはリスクがあり、成果を保証するものではありません。
  </div>

  <!-- ── HEADER ──────────────────────── -->
  <header class="site-header">
    <div class="container header-inner">
      <a class="brand" href="/">
        <svg width="28" height="28" viewBox="0 0 40 40" fill="none">
          <rect x="4"  y="22" width="6" height="14" rx="1" fill="#0B1942"/>
          <rect x="13" y="16" width="6" height="20" rx="1" fill="#0B1942"/>
          <rect x="22" y="10" width="6" height="26" rx="1" fill="#0B1942"/>
          <path d="M6 22 L16 14 L24 18 L36 6" stroke="#C9861A" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M30 6 L36 6 L36 12" stroke="#C9861A" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span class="brand-text">田中蓮のマネーブログ</span>
      </a>
      <nav class="nav">
        {["学ぶ","稼ぐ","始める","節約","投資"].map(n => (
          <a href="#" class="nav-link">{n}</a>
        ))}
      </nav>
    </div>
  </header>

  <!-- ── HERO — Split panel ──────────── -->
  <section class="hero">
    <!-- Left: editorial dark -->
    <div class="hero-left">
      <div class="hero-issue">Vol. 01 — 田中蓮のマネーブログ</div>
      <h1 class="hero-h1">
        借金とお金の不安を、<br/>
        <em>知識</em>で立て直す。
      </h1>
      <p class="hero-lead">
        ブログと動画で「学ぶ・実践する・続ける」。<br/>
        田中蓮のマネーブログは、初心者・再出発の方のための<br/>
        無料の総合メディアです。
        <span class="hero-disclaimer">※当サイトはアフィリエイト広告を含みます。成果を保証するものではありません。</span>
      </p>
      <div class="hero-cta-row">
        <a class="btn btn-primary btn-lg" href="/blog/nisa-hajimekata/">
          まずは記事で学ぶ <span class="arrow">→</span>
        </a>
        <a class="btn btn-ghost btn-lg" href="#compare">
          サービスを比較する
        </a>
      </div>
      <div class="hero-stats">
        <div class="hero-stat">
          <span class="hero-stat-num">200万</span>
          <span class="hero-stat-label">借金から<br/>スタート</span>
        </div>
        <div class="hero-stat">
          <span class="hero-stat-num">月20万</span>
          <span class="hero-stat-label">副業収入を<br/>達成</span>
        </div>
        <div class="hero-stat">
          <span class="hero-stat-num">500万</span>
          <span class="hero-stat-label">資産形成<br/>達成</span>
        </div>
      </div>
    </div>

    <!-- Right: amber manga panel -->
    <div class="hero-right">
      <div class="hero-char-wrap">
        <div class="hero-char-caption">田中 蓮・32歳 IT会社員</div>
        <!-- Character bust SVG — confident smile -->
        <svg viewBox="0 0 240 300" width="100%" style="max-width:280px;display:block;margin:0 auto;">
          <!-- Background circle glow -->
          <circle cx="120" cy="140" r="100" fill="rgba(184,120,14,.12)"/>
          <!-- Shadow -->
          <ellipse cx="120" cy="295" rx="80" ry="8" fill="rgba(0,0,0,.12)"/>
          <!-- Body / suit -->
          <path d="M20 300 L52 200 Q80 212 80 230 L120 245 L160 230 Q160 212 188 200 L220 300 Z" fill="#0B1942"/>
          <!-- White shirt -->
          <path d="M80 230 L120 218 L160 230 L160 248 Q120 238 80 248 Z" fill="#fff"/>
          <!-- Lapels -->
          <path d="M80 230 L92 260" stroke="#071030" stroke-width="1.5"/>
          <path d="M160 230 L148 260" stroke="#071030" stroke-width="1.5"/>
          <!-- Neck -->
          <rect x="104" y="170" width="32" height="34" rx="4" fill="#F4D5BA"/>
          <!-- Head -->
          <ellipse cx="120" cy="130" rx="48" ry="54" fill="#F4D5BA"/>
          <!-- Hair -->
          <path d="M72 118 Q72 64 120 64 Q168 64 168 118 Q168 96 158 82 Q144 68 120 72 Q96 76 84 88 Q76 98 72 118Z" fill="#1A1A1A"/>
          <path d="M84 88 Q98 76 120 76 Q142 76 158 88 L158 72 Q142 60 120 60 Q98 60 84 72 Z" fill="#0A0A0A"/>
          <!-- Eyes — confident, slight squint -->
          <ellipse cx="103" cy="130" rx="5" ry="6.5" fill="#0A0A0A"/>
          <ellipse cx="137" cy="130" rx="5" ry="6.5" fill="#0A0A0A"/>
          <ellipse cx="104.5" cy="128.5" rx="1.8" ry="2" fill="#fff"/>
          <ellipse cx="138.5" cy="128.5" rx="1.8" ry="2" fill="#fff"/>
          <!-- Brows -->
          <path d="M95 118 L112 120" stroke="#1A1A1A" stroke-width="3" stroke-linecap="round"/>
          <path d="M128 120 L145 118" stroke="#1A1A1A" stroke-width="3" stroke-linecap="round"/>
          <!-- Cheeks -->
          <ellipse cx="90" cy="142" rx="7" ry="4" fill="#F2A491" opacity=".55"/>
          <ellipse cx="150" cy="142" rx="7" ry="4" fill="#F2A491" opacity=".55"/>
          <!-- Smile -->
          <path d="M104 152 Q120 166 136 152" stroke="#8B4A3C" stroke-width="3" fill="#FFC0B0" stroke-linecap="round"/>
          <!-- Decoration: sparkle near head -->
          <path d="M176 72 L178 66 L180 72 L186 74 L180 76 L178 82 L176 76 L170 74 Z" fill="#E8940F"/>
          <path d="M56 90 L57.5 85 L59 90 L64 91.5 L59 93 L57.5 98 L56 93 L51 91.5 Z" fill="#E8940F" opacity=".7"/>
        </svg>
      </div>
    </div>
  </section>

  <!-- Sub-banner -->
  <div class="container subbanner-wrap">
    <div class="subbanner reveal">
      <div class="sb-left">
        <div class="sb-icon">
          <svg width="30" height="30" viewBox="0 0 48 48" fill="none" stroke="#0B1942" stroke-width="2" stroke-linejoin="round" stroke-linecap="round">
            <path d="M6 10 Q14 8 22 12 L24 40 Q14 36 6 38 Z" fill="#EEF1F7"/>
            <path d="M42 10 Q34 8 26 12 L24 40 Q34 36 42 38 Z" fill="#EEF1F7"/>
            <path d="M24 12 L24 40"/>
          </svg>
        </div>
        <div>
          <div class="sb-title">学ぶ・知る・行動するを応援する</div>
          <div class="sb-sub">お金の知識から実践まで、わかりやすくサポートします。</div>
        </div>
      </div>
      <a class="btn btn-navy" href="/blog/">人気の最新記事を読む →</a>
    </div>
  </div>

  <!-- ── PROBLEM SECTION ─────────────── -->
  <section class="section">
    <div class="container">
      <div class="section-head reveal">
        <div class="editorial-label">Problem</div>
        <h2 class="section-title">こんな悩みを<br/>抱えていませんか？</h2>
        <p class="section-sub">多くの方が、まず「知識の壁」にぶつかります。</p>
      </div>
      <div class="problem-grid">
        <div class="problem-card reveal">
          <div class="problem-num">01</div>
          <div class="problem-icon">💸</div>
          <div class="problem-title">給料だけでは将来が<br/>不安で仕方ない</div>
          <div class="problem-body">毎月何となく使って、気づけばほとんど残っていない。老後2000万円問題も他人事じゃない。</div>
        </div>
        <div class="problem-card reveal" style="animation-delay:.1s">
          <div class="problem-num">02</div>
          <div class="problem-icon">📊</div>
          <div class="problem-title">投資を始めたいけど<br/>怖くて踏み出せない</div>
          <div class="problem-body">NISAやiDeCoの言葉は知っているのに、損したらどうしようという恐怖が先に来る。</div>
        </div>
        <div class="problem-card reveal" style="animation-delay:.2s">
          <div class="problem-num">03</div>
          <div class="problem-icon">🔄</div>
          <div class="problem-title">情報が多すぎて<br/>何から始めればいいか謎</div>
          <div class="problem-body">YouTube・ブログ・SNS、みんな言うことが違う。正しい情報を選ぶ基準がわからない。</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── MANGA STORY ─────────────────── -->
  <section class="section section--paper">
    <div class="container">
      <div class="section-head reveal">
        <div class="editorial-label">Story</div>
        <h2 class="section-title">4コマでわかる<br/>お金の改善ストーリー</h2>
        <p class="section-sub">借金200万円から資産500万円へ。田中蓮の実話（架空キャラクター）。</p>
      </div>

      <div class="manga-img-wrap reveal">
        <img
          src="/images/yonkoma.jpg"
          alt="4コマでわかるお金の改善ストーリー：借金200万円→どん底の毎日→きっかけとの出会い→資産500万円達成！"
          class="manga-strip-img"
          width="1081"
          height="389"
          loading="lazy"
        />
      </div>
    </div>
  </section>

  <!-- ── ACHIEVEMENT BAR ─────────────── -->
  <div class="achievement-bar">
    <div class="container">
      <div class="achievement-inner">
        <div class="achievement-item reveal">
          <span class="achievement-num">借金→０</span>
          <span class="achievement-label">借金200万円を完済<br/>知識と節約の力で</span>
        </div>
        <div class="achievement-item reveal" style="animation-delay:.12s">
          <span class="achievement-num">月+20万</span>
          <span class="achievement-label">副業収入を達成<br/>ブログ・アフィリエイトで</span>
        </div>
        <div class="achievement-item reveal" style="animation-delay:.24s">
          <span class="achievement-num">資産500万</span>
          <span class="achievement-label">NISA・投資で形成<br/>3年間のコツコツ継続</span>
        </div>
        <div class="achievement-item reveal" style="animation-delay:.36s">
          <span class="achievement-num">35歳FIRE</span>
          <span class="achievement-label">経済的自由への道<br/>今も進行中</span>
        </div>
      </div>
    </div>
  </div>

  <!-- ── THEMES ─────────────────────── -->
  <section class="section">
    <div class="container">
      <div class="section-head reveal">
        <div class="editorial-label">Learn</div>
        <h2 class="section-title">お金の不安を解消する<br/>4つの学習テーマ</h2>
        <p class="section-sub">まずは自分に合うテーマから、今日からの一歩を踏み出しましょう。</p>
      </div>
      <div class="themes-grid">
        <div class="theme-card reveal">
          <div class="theme-icon-wrap">
            <svg width="28" height="28" viewBox="0 0 48 48" fill="none">
              <rect x="6" y="14" width="36" height="26" rx="3" fill="#B8780E" stroke="#8B5E0A" stroke-width="1.5"/>
              <path d="M6 14 L34 8 L42 14" fill="#8B5E0A"/>
              <circle cx="34" cy="27" r="3" fill="#FBBF24" stroke="#8B5E0A"/>
            </svg>
          </div>
          <div class="theme-title">お金の基礎</div>
          <div class="theme-body">知識ゼロから始めたい方に。家計簿の付け方、収支管理、今日から使える小さな習慣。</div>
        </div>
        <div class="theme-card reveal" style="animation-delay:.08s">
          <div class="theme-icon-wrap">
            <svg width="28" height="28" viewBox="0 0 48 48" fill="none">
              <rect x="6"  y="28" width="7" height="14" fill="#0B1942"/>
              <rect x="16" y="20" width="7" height="22" fill="#0B1942"/>
              <rect x="26" y="12" width="7" height="30" fill="#0B1942"/>
              <path d="M8 26 L20 18 L28 22 L42 8" stroke="#1E6B45" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
              <path d="M36 8 L42 8 L42 14" stroke="#1E6B45" stroke-width="3" stroke-linecap="round"/>
            </svg>
          </div>
          <div class="theme-title">副業</div>
          <div class="theme-body">在宅でできる副業から始めて、収入の柱を増やす実践的な情報を紹介。</div>
        </div>
        <div class="theme-card reveal" style="animation-delay:.16s">
          <div class="theme-icon-wrap">
            <svg width="28" height="28" viewBox="0 0 48 48" fill="none">
              <rect x="10" y="6" width="24" height="32" rx="2" fill="#fff" stroke="#0B1942" stroke-width="1.5"/>
              <path d="M14 14 H30 M14 20 H30 M14 26 H24" stroke="#0B1942" stroke-width="1.5" stroke-linecap="round"/>
              <circle cx="36" cy="34" r="8" fill="#FBBF24" stroke="#fff" stroke-width="2"/>
              <text x="36" y="38" text-anchor="middle" font-family="Noto Sans JP" font-weight="900" font-size="10" fill="#7A4A00">¥</text>
            </svg>
          </div>
          <div class="theme-title">資産形成</div>
          <div class="theme-body">つみたてNISA・iDeCo・投資信託。資産を着実に増やすための情報を発信。</div>
        </div>
        <div class="theme-card reveal" style="animation-delay:.24s">
          <div class="theme-icon-wrap">
            <svg width="28" height="28" viewBox="0 0 48 48" fill="none" stroke="#0B1942" stroke-width="1.5">
              <rect x="8" y="10" width="32" height="22" rx="2" fill="#EEF1F7"/>
              <path d="M4 36 H44 L41 40 H7 Z" fill="#0B1942"/>
              <rect x="14" y="22" width="3" height="6" fill="#0B1942"/>
              <rect x="20" y="18" width="3" height="10" fill="#0B1942"/>
              <rect x="26" y="14" width="3" height="14" fill="#0B1942"/>
              <path d="M14 24 L22 18 L28 20 L34 14" stroke="#1E6B45" stroke-width="2" fill="none"/>
            </svg>
          </div>
          <div class="theme-title">借金・返済</div>
          <div class="theme-body">借金の見直し、返し方、相談先など、安心して立て直すための整理済み情報。</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── ARTICLES ───────────────────── -->
  <section class="section section--tint">
    <div class="container">
      <div class="section-head reveal">
        <div class="editorial-label">Articles</div>
        <h2 class="section-title">体験談・解説ベースで<br/>学べる人気記事</h2>
        <p class="section-sub">リアルな経験から学べる、初心者にも読みやすい記事を集めました。</p>
      </div>
      <div class="articles-grid">
        {allPosts.map((post, i) => {
          const cs = catStyle[post.data.category] ?? { cls: 'cat-invest', bg: 'linear-gradient(135deg,#DBEAFE,#93C5FD)' };
          const bg = cs.bg;
          const cls = cs.cls;
          const dateStr = post.data.pubDate.toLocaleDateString('ja-JP', { year: 'numeric', month: '2-digit', day: '2-digit' });
          return (
            <article class={`article-card reveal`} style={`animation-delay:${i * 0.08}s`}>
              <a href={`/blog/${post.id}/`} style="display:block;color:inherit;text-decoration:none;">
                <div class="article-thumb" style={`background:${bg}`}>
                  <svg viewBox="0 0 320 200" xmlns="http://www.w3.org/2000/svg">
                    {post.data.category === 'NISA・投資' && (
                      <>
                        <rect x="90" y="90" width="16" height="80" rx="2" fill="#0B1942" opacity=".6"/>
                        <rect x="112" y="70" width="16" height="100" rx="2" fill="#0B1942" opacity=".6"/>
                        <rect x="134" y="48" width="16" height="122" rx="2" fill="#0B1942" opacity=".6"/>
                        <rect x="156" y="28" width="16" height="142" rx="2" fill="#1E6B45"/>
                        <path d="M96 88 L118 68 L140 46 L164 28" stroke="#16A34A" stroke-width="5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
                        <path d="M158 28 L164 28 L164 36" stroke="#16A34A" stroke-width="5" fill="none" stroke-linecap="round"/>
                        <text x="240" y="120" text-anchor="middle" font-family="Noto Sans JP" font-weight="900" font-size="28" fill="#0B1942" opacity=".12">NISA</text>
                      </>
                    )}
                    {post.data.category === '副業・節税' && (
                      <>
                        <rect x="56" y="30" width="188" height="112" rx="5" fill="#1E293B"/>
                        <rect x="64" y="38" width="172" height="96" fill="#fff"/>
                        <rect x="82" y="88" width="10" height="38" fill="#1E293B"/>
                        <rect x="98" y="72" width="10" height="54" fill="#1E293B"/>
                        <rect x="114" y="56" width="10" height="70" fill="#1E293B"/>
                        <path d="M84 92 L104 74 L120 60 L140 50" stroke="#16A34A" stroke-width="3.5" fill="none" stroke-linecap="round"/>
                        <rect x="30" y="150" width="260" height="12" rx="2" fill="#0F172A"/>
                        <circle cx="218" cy="86" r="22" fill="#FBBF24" opacity=".75"/>
                        <text x="218" y="93" text-anchor="middle" font-family="Noto Sans JP" font-weight="900" font-size="18" fill="#92400E">¥</text>
                      </>
                    )}
                    {post.data.category === 'お得情報' && (
                      <>
                        <rect x="60" y="46" width="200" height="114" rx="10" fill="#0B1942"/>
                        <rect x="60" y="82" width="200" height="34" fill="#162254"/>
                        <rect x="76" y="108" width="70" height="22" rx="4" fill="rgba(255,255,255,.35)"/>
                        <rect x="156" y="108" width="48" height="22" rx="4" fill="rgba(255,255,255,.25)"/>
                        <circle cx="228" cy="64" r="16" fill="#FBBF24" opacity=".7"/>
                        <circle cx="244" cy="64" r="16" fill="#F97316" opacity=".7"/>
                        <text x="76" y="68" font-family="Noto Sans JP" font-weight="900" font-size="12" fill="rgba(255,255,255,.55)">CARD</text>
                      </>
                    )}
                    {post.data.category === 'FX・外貨' && (
                      <>
                        <line x1="92" y1="40" x2="92" y2="150" stroke="#CBD5E1" stroke-width="1.5"/>
                        <line x1="132" y1="50" x2="132" y2="155" stroke="#CBD5E1" stroke-width="1.5"/>
                        <line x1="172" y1="35" x2="172" y2="145" stroke="#CBD5E1" stroke-width="1.5"/>
                        <line x1="212" y1="65" x2="212" y2="148" stroke="#CBD5E1" stroke-width="1.5"/>
                        <rect x="80" y="65" width="24" height="55" rx="2" fill="#16A34A"/>
                        <rect x="120" y="90" width="24" height="42" rx="2" fill="#DC2626"/>
                        <rect x="160" y="40" width="24" height="65" rx="2" fill="#16A34A"/>
                        <rect x="200" y="78" width="24" height="44" rx="2" fill="#DC2626"/>
                        <text x="160" y="175" text-anchor="middle" font-family="Noto Sans JP" font-size="12" fill="#6B6560">為替・FX</text>
                      </>
                    )}
                  </svg>
                </div>
                <div class="article-body">
                  <span class={`article-cat ${cls}`}>{post.data.category}</span>
                  <h3 class="article-title">{post.data.title}</h3>
                  <div class="article-meta">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#6B6560" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7 V12 L15 14" stroke-linecap="round"/></svg>
                    <span>{dateStr}</span>
                  </div>
                </div>
              </a>
            </article>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ── REASONS ───────────────────── -->
  <section class="section section--paper">
    <div class="container">
      <div class="section-head reveal">
        <div class="editorial-label">Why Us</div>
        <h2 class="section-title">このブログが<br/>選ばれる理由</h2>
      </div>
      <div class="reasons-grid">
        <div class="reason-card reveal">
          <div class="reason-badge">初心者にやさしい</div>
          <div class="reason-icon-wrap">
            <svg width="48" height="48" viewBox="0 0 120 120">
              <defs><clipPath id="rc1"><circle cx="60" cy="60" r="56"/></clipPath></defs>
              <g clip-path="url(#rc1)">
                <rect width="120" height="120" fill="#EEF1F7"/>
                <rect x="50" y="62" width="20" height="20" fill="#F4D5BA"/>
                <path d="M30 92 Q60 80 90 92 L100 120 L20 120 Z" fill="#fff"/>
                <path d="M18 120 L30 86 Q42 94 42 110 L60 120 Z" fill="#0B1942"/>
                <path d="M102 120 L90 86 Q78 94 78 110 L60 120 Z" fill="#0B1942"/>
                <ellipse cx="60" cy="52" rx="22" ry="26" fill="#F4D5BA"/>
                <path d="M38 46 Q38 22 60 22 Q82 22 82 46 Q82 38 76 34 Q70 30 60 32 Q50 34 44 40 Q40 44 38 46Z" fill="#1A1A1A"/>
                <ellipse cx="51" cy="54" rx="2.2" ry="3" fill="#111"/>
                <ellipse cx="69" cy="54" rx="2.2" ry="3" fill="#111"/>
                <path d="M55 66 Q60 70 65 66" stroke="#9B5A4A" stroke-width="1.8" stroke-linecap="round" fill="none"/>
              </g>
            </svg>
          </div>
          <p class="reason-text">専門用語はなるべく使わず、やさしく丁寧に解説します。</p>
        </div>
        <div class="reason-card reveal" style="animation-delay:.08s">
          <div class="reason-badge">実体験ベース</div>
          <div class="reason-icon-wrap">
            <svg width="40" height="40" viewBox="0 0 32 32" fill="none">
              <path d="M16 28 C 6 20, 4 14, 8 10 C 12 6, 16 10, 16 12 C 16 10, 20 6, 24 10 C 28 14, 26 20, 16 28Z" fill="#C0392B"/>
            </svg>
          </div>
          <p class="reason-text">実際に経験したことだけを、誠実にお届けします。</p>
        </div>
        <div class="reason-card reveal" style="animation-delay:.16s">
          <div class="reason-badge">情報の透明性</div>
          <div class="reason-icon-wrap">
            <svg width="36" height="36" viewBox="0 0 32 32" fill="none" stroke="#0B1942" stroke-width="2" stroke-linejoin="round">
              <path d="M8 4 H20 L26 10 V28 H8 Z" fill="#EEF1F7"/>
              <path d="M20 4 V10 H26"/>
              <path d="M12 16 H22 M12 20 H22 M12 24 H18" stroke-linecap="round"/>
            </svg>
          </div>
          <p class="reason-text">根拠・写真・データを必ず添えて信頼性を担保します。</p>
        </div>
        <div class="reason-card reveal" style="animation-delay:.24s">
          <div class="reason-badge">行動を後押し</div>
          <div class="reason-icon-wrap">
            <svg width="36" height="36" viewBox="0 0 32 32" fill="none">
              <path d="M8 4 V28" stroke="#0B1942" stroke-width="2.4" stroke-linecap="round"/>
              <path d="M8 6 H24 L20 12 L24 18 H8 Z" fill="#0B1942"/>
            </svg>
          </div>
          <p class="reason-text">今日から始められる小さな一歩をいつも用意しています。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ── COMPARE ───────────────────── -->
  <section class="section" id="compare">
    <div class="container">
      <div class="section-head reveal">
        <div class="editorial-label">Compare</div>
        <h2 class="section-title">目的別おすすめ<br/>サービス比較</h2>
        <p class="section-sub">あなたに合ったサービスを目的別に比較できます。<small style="color:var(--amber)">※PRを含みます</small></p>
      </div>
      <div class="compare-wrap reveal">
        <table class="compare-table">
          <thead>
            <tr>
              <th style="text-align:left">カテゴリ</th>
              <th>節約・家計</th>
              <th>資産形成</th>
              <th>副収入を増やしたい</th>
              <th style="width:150px">公式サイト</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>家計管理</td>
              <td>マネーフォワード ME</td>
              <td>マネーフォワード ME</td>
              <td>ZAIM</td>
              <td><a class="btn-compare" href="#">公式を見る →</a></td>
            </tr>
            <tr>
              <td>証券口座</td>
              <td>SBI証券</td>
              <td class="highlight-cell">楽天証券</td>
              <td>松井証券</td>
              <td><a class="btn-compare" href="/blog/sbi-rakuten-hikaku/">比較記事を読む</a></td>
            </tr>
            <tr>
              <td>NISA</td>
              <td>つみたてNISA</td>
              <td class="highlight-cell">つみたてNISA</td>
              <td>ロボアドバイザー</td>
              <td><a class="btn-compare" href="/blog/nisa-hajimekata/">始め方を読む</a></td>
            </tr>
            <tr>
              <td>FX口座</td>
              <td>—</td>
              <td>GMOクリック証券</td>
              <td class="highlight-cell">DMM FX</td>
              <td><a class="btn-compare" href="/blog/fx-kouza-hikaku/">FX比較を読む</a></td>
            </tr>
            <tr>
              <td>副業・スキル</td>
              <td>Udemy</td>
              <td>ポイント投資</td>
              <td class="highlight-cell">ブログ副業</td>
              <td><a class="btn-compare" href="#">始め方を読む</a></td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- ── FAQ ──────────────────────── -->
  <section class="section section--paper">
    <div class="container--narrow">
      <div class="section-head reveal">
        <div class="editorial-label">FAQ</div>
        <h2 class="section-title">よくある質問</h2>
      </div>
      <div class="faq-list reveal">
        <details class="faq-item">
          <summary>
            <div class="faq-q-icon">Q</div>
            <span class="faq-q-text">このブログのお金の知識はどこで学べますか？</span>
            <span class="faq-toggle">+</span>
          </summary>
          <div class="faq-a">「学ぶ」カテゴリーから、家計管理・投資・借金返済まで、初心者向けの記事を順番に読み進められます。</div>
        </details>
        <details class="faq-item">
          <summary>
            <div class="faq-q-icon">Q</div>
            <span class="faq-q-text">初心者でも理解しやすく書かれていますか？</span>
            <span class="faq-toggle">+</span>
          </summary>
          <div class="faq-a">専門用語にはすべて補足を添え、図解と実例を交えて、ゼロから読めるように構成しています。</div>
        </details>
        <details class="faq-item">
          <summary>
            <div class="faq-q-icon">Q</div>
            <span class="faq-q-text">アフィリエイト広告はどのように扱われていますか？</span>
            <span class="faq-toggle">+</span>
          </summary>
          <div class="faq-a">PR表記を必ず明示し、実際に使ったサービスのみを推奨しています。広告主から内容指示を受けることはありません。</div>
        </details>
        <details class="faq-item">
          <summary>
            <div class="faq-q-icon">Q</div>
            <span class="faq-q-text">記事の内容はどのように作られていますか？</span>
            <span class="faq-toggle">+</span>
          </summary>
          <div class="faq-a">運営者の実体験と、一次情報・公的データに基づき編集しています。情報の更新日を記事下部に表示しています。</div>
        </details>
      </div>
    </div>
  </section>

  <!-- ── FINAL CTA ─────────────────── -->
  <section class="finalcta">
    <div class="finalcta-inner">
      <span class="finalcta-eyebrow">Start Today</span>
      <h2 class="finalcta-h">お金の不安を、<br/>今日から少しずつ減らそう。</h2>
      <p class="finalcta-sub">知識と行動が、あなたの未来を支えていきます。</p>
      <div class="finalcta-row">
        <a class="btn btn-primary btn-lg" href="/blog/">
          まずは記事で学ぶ <span class="arrow">→</span>
        </a>
        <a class="btn btn-ghost btn-lg" href="#compare">
          サービスを比較する
        </a>
        <a class="btn btn-sage btn-lg" href="#">
          LINEで相談する
        </a>
      </div>
    </div>
  </section>

  <!-- ── FOOTER ───────────────────── -->
  <footer class="footer">
    <div class="container footer-grid">
      <div>
        <div class="footer-h">運営者について</div>
        <ul class="footer-list">
          <li><a href="/about">運営者プロフィール</a></li>
          <li><a href="/about">運営理念</a></li>
          <li><a href="/about">お問い合わせ</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-h">学ぶカテゴリ</div>
        <ul class="footer-list">
          <li><a href="/blog/">お金の基礎</a></li>
          <li><a href="/blog/">節約・家計</a></li>
          <li><a href="/blog/">投資・資産形成</a></li>
          <li><a href="/blog/">借金・返済</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-h">PR・免責</div>
        <ul class="footer-list">
          <li><a href="/about">広告掲載基準</a></li>
          <li><a href="/disclaimer">免責事項</a></li>
          <li><a href="/privacy">プライバシーポリシー</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-h">LINE相談窓口</div>
        <ul class="footer-list">
          <li><a href="#">登録はこちら</a></li>
          <li><a href="#">お知らせ</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <div class="container footer-bottom-inner">
        <div class="footer-brand">
          <svg width="20" height="20" viewBox="0 0 40 40" fill="none">
            <rect x="4"  y="22" width="6" height="14" rx="1" fill="#4B6CB7"/>
            <rect x="13" y="16" width="6" height="20" rx="1" fill="#4B6CB7"/>
            <rect x="22" y="10" width="6" height="26" rx="1" fill="#4B6CB7"/>
            <path d="M6 22 L16 14 L24 18 L36 6" stroke="#C9861A" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M30 6 L36 6 L36 12" stroke="#C9861A" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>田中蓮のマネーブログ</span>
        </div>
        <div class="footer-copy">© 2025 田中蓮のマネーブログ. All rights reserved.</div>
      </div>
    </div>
  </footer>

  <!-- Sticky CTA (mobile) -->
  <div class="sticky-cta" id="stickyCta">
    <a class="btn btn-primary" href="/blog/nisa-hajimekata/">
      まずは無料で記事を読む →
    </a>
  </div>

  <script>
    // FAQ toggle label
    document.querySelectorAll('.faq-item').forEach(el => {
      el.addEventListener('toggle', () => {
        const t = el.querySelector('.faq-toggle');
        if (t) t.textContent = el.open ? '×' : '+';
      });
    });

    // Scroll reveal with IntersectionObserver
    const reveals = document.querySelectorAll('.reveal');
    if ('IntersectionObserver' in window) {
      const io = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            io.unobserve(entry.target);
          }
        });
      }, { threshold: 0.1 });
      reveals.forEach(el => io.observe(el));
    } else {
      reveals.forEach(el => el.classList.add('is-visible'));
    }

    // Sticky CTA: hide when hero is visible
    const hero = document.querySelector('.hero');
    const sticky = document.getElementById('stickyCta');
    if (hero && sticky) {
      const heroObs = new IntersectionObserver(([e]) => {
        sticky.style.opacity = e.isIntersecting ? '0' : '1';
        sticky.style.pointerEvents = e.isIntersecting ? 'none' : 'auto';
      }, { threshold: 0.2 });
      heroObs.observe(hero);
    }
  </script>

</body>
</html>
