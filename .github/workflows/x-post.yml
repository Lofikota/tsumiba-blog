name: X自動投稿

on:
  schedule:
    # JST 07:30 = UTC 22:30（前日）
    - cron: '30 22 * * *'
    # JST 12:00 = UTC 03:00
    - cron: '0 3 * * *'
    # JST 19:00 = UTC 10:00
    - cron: '0 10 * * *'
    # JST 21:00 = UTC 12:00
    - cron: '0 12 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'ドライラン（投稿せずキューだけ確認）'
        required: false
        default: 'false'

jobs:
  post-tweet:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 依存関係インストール
        run: pip install -r x-automation/requirements.txt

      - name: X投稿実行
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: python x-automation/x_poster.py

      - name: キュー更新をコミット
        env:
          NOW_JST: ${{ env.NOW_JST }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add x-automation/data/tweet_queue.csv
          TIMESTAMP=$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M')
          git diff --staged --quiet || git commit -m "chore: X投稿キュー更新 ${TIMESTAMP} JST"
          git push
